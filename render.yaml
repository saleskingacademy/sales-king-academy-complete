services:
  - type: web
    name: ska-autonomous-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ska_backend_complete:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: SQUARE_ACCESS_TOKEN
        sync: false
